  ransomlook:
    build: .
    working_dir: /ransomlook
    tty: true
    command:
        - /bin/sh
        - -c
        - |
            service tor start
            poetry run scrape
            poetry run parse
    volumes:
        - ./config:/ransomlook/config
        - ./data:/ransomlook/data
        - ./docs:/ransomlook/docs
        - ./source:/ransomlook/source
